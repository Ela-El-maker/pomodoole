title = "Petal Focus gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Firebase client config keys in app bundles are public identifiers, not server secrets"
paths = [
  '''android/app/google-services\.json''',
  '''ios/Runner/GoogleService-Info\.plist''',
  '''lib/firebase_options\.dart'''
]
